server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vicentemr/microservicios-demo.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user: root
    password: root
